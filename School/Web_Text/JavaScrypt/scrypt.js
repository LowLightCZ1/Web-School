const color = ["black", "gray"]
const header = [...document.getElementsByTagName("header")]

window.onscroll = () => {
    const scrollFromTop = window.pageXOffset
    for (let i= 0, header.length,  )
}